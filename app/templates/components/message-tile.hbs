<p>
  {{#link-to 'message' message.id}}{{message.riddle}} <br> asked by ~{{message.author}}{{/link-to}}
  {{#if isImageShowing}}
    <p>
      <img src= {{message.image}} alt= {{message.hint}} {{action 'imageHide'}}>
      <br>
      <hr>
    </p>
  {{else}}
    <button class="btn btn-info" {{action 'imageShow'}}>Image</button>
  {{/if}}
</p>
